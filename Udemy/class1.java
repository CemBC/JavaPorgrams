package Udemy;//import java.util.Scanner;

public class class1 {
    public static void main(String[] args) {

        /*  System.out.printf("Hello %s %d %04d %.3f","SE",115,1,0.5675674545);  */

        // byte ---> Short ---> Int ---> Long
        // 8 bit     16 bit     32 bit    64 bit
        // Flout --> Double
        //32 bit     64 bit
        //char => 2 bit

        //Boolean = True,False
        //postfix güncellenmeyen değer
        //prefix güncellenen değer

        //Scanner inp = new Scanner(System.in)

        // ==  // !=   // <=  // >=  // > // <
        // && = and operator
        // || = or operator

        // unicode yazılırken u küçük + yok başına da \ konulur

        // !x means true if x = false, means false if x = true

        //int olarak 1/4 = 0, ama böyle yazarsak (float) 1 / (float)4 = 0.25 olur

        // System.out.printf("Hello %s, %03d , %d , %.3f\n ","SE 115",13,1,2.12321321);
        // --> Hello Se 115, 013 ,1 , 2.123
        // System.out.printf("Hello %s, %d , %02d , %.3f\n ","SE 115",13,1,2.12321321);
        // --> Hello SE 115 , 13 , 01 , 2.123

        //("He said \"hello\" to me") = yoksa arada belirsiz değer kalır o yüzden böyle yazılır

        /* for (başlatma ; koşul ; arttırma veya azaltma işlemler ) {
               } */
         /* int i;
        for ( i = 0 ; i < 5 ; i++)
        {
            System.out.println(i);
        }
        output= 0
                1
                2
                3
                4

        for (int i = 0 ; i < 5 ; i++) {
            System.out.println(i);
        } */
        /* int i1;
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a Float = ");
        float a = sc.nextFloat();
        i1 = (int)a;
        System.out.println(i1);   */
        /*     do {  *****************
        ******************
        } while (***);
                */
        /* String linet = "";
        for(int i = 0 ; i < 10 ; i++) {
            linet = linet + " ";
        }
        System.out.println(linet); */

        /* if(boolean) the process ; */

         /*public static int power(int b , int p) {
        if (p !=  0) {
            return b*power(b,p-1);
    }else {
            return 1;
        }*/

    /* parantez
    postfix x++
    prefix ++x
    çarpma bölme
    toplama çıkarma
    relations
    eşitlikler
    and
    or
    casting
     */


    }






}

